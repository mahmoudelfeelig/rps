{
  "name": "rps-root",
  "private": true,
    "scripts": {
"dev": "concurrently \"cd frontend && set HOST=localhost&& npm start\" \"cd backend && npm run dev\"",
      "install-all": "npm install && cd frontend && npm install && cd ..\\..\\backend && npm install"
    },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}