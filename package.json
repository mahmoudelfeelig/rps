{
  "name": "rps-root",
  "private": true,
    "scripts": {
"dev": "concurrently \"cd frontend\\bet-economy && set HOST=localhost&& npm start\" \"cd backend && npm run dev\"",
      "install-all": "npm install && cd frontend\\bet-economy && npm install && cd ..\\..\\backend && npm install"
    },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}